/*
 * MppCol Messages
 *
 * This contains all the text for the MppCol component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.MppCol';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MppCol component!',
  },
});
