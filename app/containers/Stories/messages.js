/*
 * Stories Messages
 *
 * This contains all the text for the Stories container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Stories';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Stories container!',
  },
});
