// UnAuthorized

const LOGIN_SUCCESS = 'Successfully logged in';
const LOGIN_FAILED = 'Failed to login. Please try again';

// Forbidden Error

const ACCESS_DENIED = 'User does not have permission to access';
const ACCESS_GRANTED = 'Successfully given access';

module.exports = {
    LOGIN_SUCCESS,
    LOGIN_FAILED,
    ACCESS_DENIED,
    ACCESS_GRANTED
}